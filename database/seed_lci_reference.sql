-- ============================================================================
-- E(ESG·탄소) 데모용 LCI 배출계수 (참고값)
--
-- ※ 리튬 탄산염(HS 283691)은 EU CBAM 비대상 품목 → CBAM 기본값이 없음.
--   따라서 E는 환경부 LCI(전과정 배출계수)로 산정한다.
-- ※ 아래 값은 정식 환경부 LCI 파일 확보 전 사용하는 공개 LCA 문헌 기반 참고값
--   (리튬 탄산염 생산 ~5.5 kgCO2e/kg, 브라인 기준 근사). 실제 파일 오면 교체.
-- 재실행 안전(해당 품목 행 삭제 후 삽입).
-- ============================================================================
DELETE FROM lci_emission_factors WHERE hs_code = '283691';

INSERT INTO lci_emission_factors (material_name, hs_code, emission_factor, unit, source)
VALUES ('리튬 탄산염', '283691', 5.5, 'kgCO2e/kg', 'LCA 참고값(환경부 LCI 대체 예정)');
