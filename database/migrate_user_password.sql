-- ============================================================================
-- 이메일+비밀번호 회원가입 지원: users.password_hash 추가 (bcrypt 해시).
-- 구글 OAuth 사용자는 NULL(비번 없음). 재실행 안전.
-- ============================================================================
ALTER TABLE users ADD COLUMN IF NOT EXISTS password_hash VARCHAR(255);
